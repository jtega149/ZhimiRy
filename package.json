{
  "name": "zhimiry",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "packages/shared"
  ],
  "scripts": {
    "dev": "npm run build -w @zhimiry/shared && concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "npm run build -w @zhimiry/shared && npm run build -w backend && npm run build -w frontend",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
